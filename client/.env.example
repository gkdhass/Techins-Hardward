# Vite Environment Variables (must be prefixed with VITE_)

# API Base URL
# In production (Vercel): use /api (same domain)
# In development: use http://localhost:5000/api (separate server)
VITE_API_BASE_URL=/api
